# Legacy pip requirements file
# 
# This project now uses uv for dependency management (see pyproject.toml).
# This file is kept for backward compatibility.
#
# For new setups, use: uv sync
# For legacy setups, use: pip install -r requirements.txt
#
fastapi
uvicorn
